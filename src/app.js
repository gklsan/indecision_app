console.log('App.js is runnging!!!!!!');

// This is the JSX code
var template = <h1>This is jsx type of code!!!!</h1>;

var appRoot = document.getElementById("app_root");

ReactDOM.render(template, appRoot);
